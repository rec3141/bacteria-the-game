# local tooling / env
.claude/
.DS_Store
*.log

# server-generated stores (never commit runtime data)
scores.json
feedback.json
scenario-queue.json
scenario-ratelimit.json

# old saved WordPress page wrapper (superseded by index.html)
game.html
